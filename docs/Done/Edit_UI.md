Make these two UI changes:
1. BACK BUTTON on analysis screen
- Add a "← Back" button at the top-left of the analysis result screen
- Clicking it returns to the previous screen (dilemma input form)
- Keep existing data intact — do not clear inputs when going back
- Style consistent with existing "← Back" buttons in other screens
2. CLEAR (×) BUTTON inside input fields
- Add a small × button inside each of these inputs:
  optionA, optionB, context
- Show the × only when the field has text (hide when empty)
- Clicking × clears only that specific field
- Position: right side inside the input box
- Style: subtle, small, gray — not distracting

Do not change any other UI elements.